import common, {
  COMMON_ACTIONS,
  getMaster,
  startProgress,
  endProgress,
  switchDispMode,
  showSnackBar,
  hideSnackBar,
  Severity
} from "modules/common";

// jest.mock("sagas/requests", () => jest.fn());

describe("action creaters", () => {
  it("GET_SETTINGS", async () => {});
});

describe("reducer", () => {
  it("GET_SETTINGS", async () => {});
});
